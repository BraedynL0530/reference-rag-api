# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
pydantic>=2.9
python-dotenv>=1.0

# --- Gemini (free tier) ---
google-genai>=1.0

# --- Persistent storage: Supabase Postgres + pgvector ---
# (replaces chromadb -- local disk doesn't survive on free container hosts)
asyncpg>=0.29
pgvector>=0.3

# --- PDF parsing (page + heading extraction + page-image rendering) ---
PyMuPDF>=1.24

# --- Notes export (PDF only -- Google Slides export was dropped) ---
reportlab>=4.2

# Audio transcription goes straight through Gemini's own audio
# understanding (app/audio/transcribe.py) -- no local Whisper/ffmpeg/torch
# dependency, which was the single heaviest thing this app put in RAM.
