services:
  reference-rag-api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: reference-rag-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
